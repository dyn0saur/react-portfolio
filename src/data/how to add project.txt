Add a new project by duplicating an entry in src/data/projects.js and verifying the detail view renders as expected. Be sure every image object includes both `src` and a `placeholder` that points to the `.thumb` variant generated by `npm run generate:thumbs` so progressive loading works consistently.
